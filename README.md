# Bank nodejs applicaiton

## To run:
 - create an ``.env`` file and provide there next variables:
 - - ``PORT`` (for example, PORT=3000); 
 - ``npm install``  - install dependencies 
 - ``npm run dev``  - run in development mode 
 - ``npm start``  - run in production mode 